language: go

addons:
  apt:
    packages:
      - build-essential
      - libx11-dev
      - libxcursor-dev
      - libwayland-dev
      - xbitmaps
      - libxmu-headers
      - libxpm-dev

go:
    - "1.12"
